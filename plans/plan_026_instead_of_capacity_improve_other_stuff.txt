#1 [done] Clean up
- remove all capacity features
    - capacity property
    - Sektor capacity calculations (and tests for those)
    - capacity UI display & modification

#2 [done] Buildings with multiple functions can have functions turned off
- [done] if a building has more than one function, each function can be turned off individually
- [done] Sektor has methods
    - deactivateFunction(buildingLocation: BuildingLocation, functionIndex: number)
    - activateFunction(buildingLocation: BuildingLocation, functionIndex: number)
- [done] a function of a building with just one function cannot be deactivated
    - it is activated initially
- [done] a building with multiple functions initially has only the first function activated
    - all other functions are deactivated
- [done] a building's function which is deactivated does not factor in any calculations of inputs/imports & outputs/exports
- [done] create Sektor tests which confirm initial activation statuses & deactivation
    - use Sektor.getSektorState to assert that only activated functions contribute to the calculation of inputs/exports
- [done] a player sees which functions are activated in the UI
    - building panel below each function contains a label
        - "Active"/"Inactive"
        - text white bold
        - label not visible for buildings with only one function
    - if a function is inactive, it's input & output numbers are gray
- [done] a player can activate or deactivate a function
    - next to the activity label is a toggle switch
    - toggle is in the on position if function is active
    - when clicked, function is activated or deactivated
    - toggle not visible for buildings with only one function
    - create UI tests for
        - initially active
        - initially inactive
        - toggling to active
        - toggling to inactive

#3 [done] Toolbar building panel clearly marks location properties on multiple functions
- [done] "Affected by" list is underneath all functions
    - each function should have it's own "Affected by" list which lists all outputs of that function affected by a location property
    - update UI tests

#4 [done] Some resources are "local"
- [done] if a resource type is local, it means it cannot be imported or exported
- [done] a resource is local if resources.md (resources.test.md for test purposes) has "local" in it's space-delimited line of properties
- [done] calculate local resources in the same way as non-local resources
    - after calculation, go through imports of local resources:
        - for each import:
            - select an activated building function of an existing building which has the same input resource
                - repeat selecting until the sum of selected input amounts equals or is greater than the import amount
            - adjust calculation as if selected functions were deactivated
        - in SektorState add property starvedFunctions[]: BuildingFunction
            - BuildingFunction = { buildingLocation: BuildingLocation, functionIndex: number }
            - put all selected functions in this array
    - in SektorState.exports remove all exports of local resource types
- [done] a player can see which buildings lack local resources
    - in map, above each building which has at least one starved function, put a warning icon
        - shape of an upwards triangle
        - bright yellow
        - with black exclamation point inside
- [done] a player can see which functions of a building lack local resources
    - in building panel next to each function name, put a warning icon
        - heroicon exclamation-triangle, solid, yellow
- [done] update warning icons on modification of SektorState

#5 A function can be blocked from deactivation
- if a Function section in building specification contains property "Active: always"
    - that function is initially active even if it is not the first function in a building specification
    - deactivation of that function does nothing
    - building panel label always shows "Active"
    - building panel does not display toggle switch
