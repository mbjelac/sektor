#1 [done] Location properties are clearly visible as an overlay
- [done] when a building is selected in the building toolbar
    - if a building output has location property
        - turn on that location property overlay on the map
    - when a building is deselected, turn off overlay
    - overlay should be same as building selection overlay
        - square around the edges of the floor
        - use color in properties.md
            - only one color is specified now
               - lowest value is black
               - highest value is that color
               - values between should be color between black and that property color

#2 [done] Fix default floor display
- [done] always display floors with soil colors as before
    - highest soil property has color #86E389
    - lowest soil property has color #E3CA86
    - property value between highest & lowest has color between those two colors

#3 Adjust geo summary panel
- only one color for a property is used now
    - display only that swatch in "geo summary" panel buttons
- panel title should be "Geography"
- if an item selected, turn on overlay for that property
- if "soil" selected, turn off overlay
