Instead of each floor having an altitude, there is no altitude, just mountain/hill squares which cannot be built upon.
They serve only as interesting visual features and obstacles for the player to build stuff.

Creation produces a matrix of mountains and hills. Each mountain/hill square is a "building" which cannot be destroyed.

Terrain features also includes water, e.g.:
- sea
- lake
- river
- pond

#1 [done] Remove all existing terrain features
- [done] remove all features which use the concept of "altitude" to draw & work with higher floor blocks
- [done] remove all modifications of location properties depending on altitude

#2 [done] Sea
- [done] when creating a sektor, some floors are sea
    - a sektor has a "terrain" matrix which elements represent map squares an their values are:
        - 0 (ground)
        - 1 (sea)
    - for now, amount of sea floors is determined by randomly selecting from this list of options:
        - 0 sea floors
        - 1/4
        - half
    - sea blocks in the terrain matrix neccessarily have these properties:
        - they are all connected to each other
            - diagonal neighbours don't count
        - at least one of them is on the edge of the matrix (1st or last row, or 1st or last column)
- [done] a sea floor is rendered as sea
    - the current prototype rendering of "water floors" needs to be extracted and used to render sea floors
- [done] nothing can be built on sea
    - if a player selectes a square to construct a building and it is a sea square
        - do not construct building
        - display error "notDryEnough"
            - same error functionalities as all other errors

#3 Elevation
- [done] when creating a sektor, some floors are "elevation" (i.e. rocks)
    - a sektor terrain matrix has a value 2 (elevation)
    - for now, amount of a sektor elevation is determined by randomly selecting from this list of options:
        - 0
        - 1/10
        - 1/5
- elevation is rendered with a special "building"
    - floor is render same as for ground squares
    - render a "buiding" on that square same as it would be if a player constructed a building
        - the building render code does not come from buildings.md
        - it comes from terrain.md
        - for now, always render "Grassy rocky outcrop"
- nothing can be built on elevation
    - if a player selectes a square to construct a building and it is an elevation square
        - do not construct building
        - display error "notFlatEnough"
            - same error functionalities as all other errors
- adjust building panel
    - when this square is clicked, the building panel opens
    - building name label reads: "Elevation"
    - delete button is not displayed
- elevations cannot be destroyed
    - if the player tries to apply the Destroy tool on elevation
        - do not destroy elevation
        - display error message "canNotDestroyElevations"
