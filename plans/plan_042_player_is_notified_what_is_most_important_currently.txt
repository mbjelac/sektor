In any point in time there is a thing to do in a sektor map which can mostly benefit the player (and the whole planet).

#1 [done] Notify player about most imported resources
- [done] when sektor map is opened and every time a global or local import or export amount changes
    - find at most 3 global imports with highest import amounts
        - their global import amount is highest
    - then display a "message" to the player
- [done] display message in the bottom of the screen
    - center
    - fixed width 800px, same for all messages
    - message is in panel
        - rounded corners
        - background color #ffe187
        - text color black
        - text left-aligned
- [done] message text in this case: "This planet needs: <resource list>"
    - the list contains the 3 (or less) resources by specified criteria, by descending global import amount
    - each resource in list is displayed as <name> <icon>
    - items in list delimited by comma + space
- [done] if this message is already shown (with different resources), replace it with this one

#2 [done] Notify player about local imports which are also global imports
- [done] when sektor map is opened and every time a global or local import or export amount changes
    - find at most 3 local imports with highest import amounts
        - which are also imported globally
    - then display a message to the player
        - same UI as in #1
        - text "Avoid importing scarse resources <resource list>"
- [done] display message in the bottom of the screen
- [done] in case of multiple messages, stack them one beneath the other with a small space between
- [done] if this message is already shown (with different resources), replace it with this one

#3 [done] Make an exception for negative resources
- [done] for messages described ni #1 & #2 negative resoureces should not be notified as "needed" globally, nor locally
    - if a sektor imports a negative resource, this is good, the sektor shouldn't avoid importing this resource
    - if globally a negative resource is imported, this is good, the planet does not need this resource

#4 [done] Added or updated message is very visible
- [done] when a message is added or it's contents is updated
    - move it to the top of the message stack
    - flash it
        - background turns to white, then fades to message background color during 1 second
            - repeat 2 times

#5 Player can easily find out which building is importing resources mentioned in messages
- resources mentioned in messages can handle mouseover events:
    - same as resources in import/export panel:
        - highlight buildings which have those resources as inputs in orange
- change import-highlight color: orange -> red
- resource in message has white background while mouseover

#6 Player can see buildings which output a resource
- either mouseover on resource in import/export panel, or resource in message
    - highlight buildings which have that resource as *output*
        - in green
