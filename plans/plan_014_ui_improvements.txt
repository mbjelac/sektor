#1 [done] State panel resources displayed alphabetically
- [done] both input & output resources in the state panel should be sorted alphabetically by resource name

#2 [done] Should not be possible to bump into input overflow
- [done] input arc label has increase & decrease buttons
    - when input amount is on maximum of that target's input
        - increase button does nothing
        - button icon turns into checkCircleIcon
    - when input amount decreased (but connection still exists)
        - increase button becomes clickable
        - increase button icon turns back into up-triangle

#3 [done] Should not be possible to bump into output overflow
- [done] input arc label has increase & decrease buttons
    - when source building has no amount of that resource to give
        - increase button does nothing
        - button icon turns into heroicon minus-circle
    - when input amount decreased (but connection still exists)
        - increase button becomes clickable
        - increase button icon turns back into up-triangle

#4 [done] It is easier to select a building to connect to
- [done] when in building selection mode, building labels which display the resource icon, available & maximum amounts also show a white icon linkIcon
    - above the current data
    - centered horizontally
    - entire label is clickable to conect to that building

#5 [done] Two buildings connected with both input and output have non-overlapping arcs
- [done] draw all output arcs with a bit higher peak than input arcs
    - output arc labels should be also shifted upwards

#6 Should be clear how much input is needed and how much output is available
- both amounts should be displayed in the input arc label
    - available amount of output for that resource is displayed in the arc label
        - this amount is practically the source buildings export amount for that resource
            - or available amount if autoExport=false
    - needed amount of input (to bring imports of that input for that building to 0) is displayed in the arc label
    - label format: "importsRemaining ← currentAmount ← exportsRemaining"
    - currentAmount should be bold
