#1 [done] Introducing floating point for location properties modifiers is too complex
- [done] all location property modifiers should be integers
    - modifiers which were generated from 0 to 1 should now be from -6 to 0
    - modifiers which were generated from 1 to 2 should now be from 0 to +6
    - applying a modifier should just sum the value to the value of the output
    - if a negative modifier is applied, the result can be a minimum of 0, not below
    - extract the modifier limits -6 & 6 so they can be easily modified to improve user experience
    - adjust all tests
- [done] positive location property modifiers are displayed with "+" prefix
    - zero modifiers are displayed as just "0"

#2 [done] Buildings which have an import are highlighted
- [done] when a player floats their mouse cursor above an import amount in the state panel
    - all buildings which have the import are highlighted
    - highlight is like the select highlight
        - is orange instead of yellow
    - import row has gray highlight, same color as building panel import row background highlight
- [done] create visual test
    - with several buildings having the same resource type imported
    - cursor hover event on state panel over that imported resource amount
- [done] only buildings whose input amounts can be increased are highlighted
    - a building is highlighted only if it can still import more of the hovered resource (remaining import > 0)
    - buildings already at maximum input for that resource are not highlighted
    - update/add visual test so at least one building is at max input and is not highlighted

#3 [done] Building function description also displays location properties which affect it
- [done] when a player selectes a building in the toolbar, the panel with function description also has a list of location properties which affect it
    - below the function table
    - list location properties which affect the building
    - each list item has the name of the property and the swatch of the maximum value
- [done] create visual test for that panel only (if it doesn't exist yet)
    - include test with a panel containing a modifier property and one without any

#4 [done] Building panel clearly labels inputs & outputs
- [done] inputs column in the building panel has label "Resource"
    - should be "Inputs"
- [done] outputs column in the building panel has no header
    - create header
        - column with resource name should have label "Outputs"
        - column with output amount should have no label
- [done] add column in output panel for amount of building's output which is exported
    - add BuildingState.exports: ResourceThroughput[]
    - create Sektor test for getting exports of a building
    - display building's exports in the output panel for each resource in it's own sub-column
        - sub-column header should be heroicon arrowUpTrayIcon
    - update snapshots
- [done] building function panel from building toolbar displays both headers
    - currently only output column has header, input column hides it
    - no need to hide input column header

#5 [done] Outputs are displayed on map
- [done] when a building is selected, display all building's outputs on the map using arcs
    - output arcs have the same color as the resource's color (same as for input arcs)
    - output arcs use a dotted line to separate them from input arcs
    - on top of the output arc is a label with the resource icon and amount of the output
- [done] create visual test for rendering map with input & output arcs

#6 [done] Destroy connection
- [done] when a connection amount is lowered to 0, the connection is removed
    - add Sektor.removeInputConnection(target: BuildingLocation, source: BuildingLocation, resource: string)
    - method removes connection from state (matched by target, source and resource)
    - method throws error if connection doesn't exist
    - add Sektor test for
        - removing input connection
        - throwing error when removing non-existing connection
    - if a connection amount is lowered to 0, call Sektor.removeInputConnection
    - if removing succeeds, remove connection arc from the map along with all widgets (arc label, triangle buttons)
    - add visual test for scenario where a connection amount is lowered to 0

#7 [done] Improve building panel
- [done] toolbar's building function panel has header for location properties
    - header should be "Affected by"
    - header style same as Inputs and outputs columns header
    - location properties text style same as inputs & outputs columns text style
- [done] building panel input & output columns should always have same width
- [done] input & output columns in building panel should be one beneath the other
    - not so in the building *function* panel used by the toolbar
    - left-to-right arrow between the columns should be hidden
    - location properties have header "Geo survey"
        - header has same style as input & output columns header
        - properties text style same as inputs & outputs columns text style
    - columns in inputs section & columns in output section should have same width
        - input resource names & icons = output resource names & icons
        - input resource amounts = output resource amounts
        - input import amounts = output export amounts
    - gray line separating geo survey should span whole width
    - geo survey section should have same left & right margins as inputs & outputs
    - width of bulding panel can now be reduced 20%

#8 [done] Indicate transitions of status more clearly
- [done] status panel should have fixed width
    - currently it's width is modified in some way
    - when status is Restrictions Exceeded, status panel is wide with empty space on the right
- [done] when status changes, status panel should flash it's background
    - background color should be the color of the status
        - white (light gray) for In Progress
        - green for Success
        - orange for Restrictions Exceeded
    - then color fades gradually during 1 second
    - this repeats 3 times

#9 [done] Leaving sektor map should be more intuitive
- [done] exit button
    - in top left (above toolbar) should be moved inside status panel and placed in bottom right
    - should be labelled "Leave"

#10 [done] Location property overlay toggler labelled more clearly
- [done] put location property overlay toggler in own panel
    - panel title: "Geo survey"
    - floating on map in the bottom left corner with small margin from edge like building panel
- [done] building toolbar should also be enclosed in a panel
    - panel title: "Construction"
    - building toolbar panel should be only as high as needed to display 6 buildings
    - floating on map in the bottom left corner with small margin from edge like building panel

