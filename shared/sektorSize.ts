// A sektor's map is square and every sektor is the same number of tiles across, so how big a sektor
// is is not something it carries around or is asked about: it is this.
export const SEKTOR_SIZE = 10;
